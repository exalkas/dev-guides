Arrays with objects filter, map reduce

find index
find which
find the sum

show mdn query example


Task: create a class
class ClassName {constructor()}

Delete an item from an array:

syntax: array.splice(indexOfItemToBeDeleted, howManyItemsToBeDeleted)
returns the deleted array
N.B. the array gets mutated (changed)

If the index of the item is not known then it needs to be found.

Task: Find index of an array item.

syntax: array.findIndex( function to find the item*: e.g. item =>)

it depends on what we know about the item:

1) an object is given




task: find the min in an array
task: find the min in an array of objects
task: find the min in an array	

task: find a max in an array which is inside an object with array

task: sort an array
task: sort an array with objects

task: add in item to an array
task: add in item to an array of objects

task: accept an unknown number of parameters in a function 


Task:
deploy to GH pages:

1. npm install gh-pages --save-dev
2. add property to the app's package.json at the top level: "homepage": "http://gitname.github.io/react-gh-pages"
3. add these to the scripts property:
"scripts": {

  "predeploy": "npm run build",
  "deploy": "gh-pages -d build"
}
4. npm run deploy

if git is not initialized:
0. create an EMPTY repo online
1. git init at local machine
2. git remote add origin https://github.com/gitname/react-gh-pages.git